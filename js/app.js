const list = [];

window.onload = function() {
        document.getElementById("add button").onclick = addItem;

};

const addItem = function() {
        let newItem = {
                "description": "",
                "priority": "low",
                "completion": "incomplete"
        };

        let input = document.querySelector(".add-an-item")

        newItem["description"] =

};
